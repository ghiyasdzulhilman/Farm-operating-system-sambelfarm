kind = "web"
previewPath = "/"
title = "Farm Management System"
version = "1.0.0"
id = "artifacts/farm-app"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22783

[services.development]
run = "pnpm --filter @workspace/farm-app run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/farm-app", "run", "build" ]
publicDir = "artifacts/farm-app/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22783"
BASE_PATH = "/"
